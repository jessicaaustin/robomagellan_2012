<launch>

    <!-- teleoperation is done manually, to send commands run
    
        go forward in a straight line:
        rostopic pub /cmd_vel geometry_msgs/Twist -r 60 '[0.5, 0, 0]' '[0, 0, 0]'

        turn in a counter-clockwise circle:
        rostopic pub /cmd_vel geometry_msgs/Twist -r 60 '[0.0, 0, 0]' '[0, 0, 1.0]'
    
        turn in a clockwise circle:
        rostopic pub /cmd_vel geometry_msgs/Twist -r 60 '[0.0, 0, 0]' '[0, 0, -1.0]'
    
	stop:
        rostopic pub /cmd_vel geometry_msgs/Twist -r 60 '[0.0, 0, 0]' '[0, 0, 0.0]'

    -->

</launch>

